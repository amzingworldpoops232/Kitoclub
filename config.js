// Backend URL (Socket.io) — only needed if frontend and backend are on different hosts.
// Leave empty for Render, Fly, or npm start (same server serves game + multiplayer).
// For Netlify frontend: set to your Render/Fly URL, e.g. https://kito-club.onrender.com
window.KITO_SERVER_URL = "";
